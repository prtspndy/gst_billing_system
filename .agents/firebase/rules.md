### Firebase Reference Project Rules

Reference project name: `firebase_authentication_app`

* Treat `firebase_authentication_app` as **READ-ONLY reference material**.
* **Do NOT modify, delete, rename, move, or overwrite any file** inside this project.
* **Do NOT run commands that modify the reference project's files or configuration.**
* Use this project **only to inspect and understand the working Firebase Authentication implementation**.
* Take reference from its Firebase configuration, dependencies, initialization, authentication logic, platform setup, and working authentication flow.
* Reuse the same approach in my **target/existing project**, adapting it to that project's architecture and file structure.
* **Never copy changes back into `firebase_authentication_app`.**
* If something needs to be changed, change it **only in the target project**, never in the reference project.
* Preserve the target project's existing UI, architecture, features, and design unless authentication integration specifically requires a change.
* Do not create a new Flutter project.

**Priority:**
`firebase_authentication_app` = **Reference Only / READ-ONLY**
Target Project = **Only Project Allowed to Be Modified**
